const Navigation = () => {
    return (
        <nav className="App-nav">
            <button>Who Am I</button>
            <button>Work</button>
            <button>Touch Base</button>
        </nav>
    )
};

export default Navigation;